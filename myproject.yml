---
# file: mproject.yml
# example to use when running ansible-playbook with --extra-vars=@myproject.yml
projects:
  - name: myproject
    basedir: ./generated
    inventory: alternate
    roles:
      - mycommonrole
      - myspecialrole
